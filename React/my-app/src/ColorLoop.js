function ColorLoop() {
    let Colors = ['red', 'green', 'blue',];
    for (let i = 0; i < Colors.length; i++) {
        const color = Colors[i];
        console.log(color)
    }
}
export default ColorLoop